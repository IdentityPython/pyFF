- when ingesting:
   - pubpath
- when update:
   - load:
      - examples/met.xrd via ingesting
- when request:
    - select
    - pipe:
        - when accept application/xml:
             - xslt:
                 stylesheet: tidy.xsl
             #- pubinfo:
             #    publisher: http://met.terena.org/
             - first
             - finalize:
                 cacheDuration: PT5H
                 validUntil: P10D
             - sign:
                 key: default.key
                 cert: default.crt
             - emit application/xml
             - break
        - when accept application/json:
             - xslt:
                 stylesheet: discojson.xsl
             - emit application/json:
             - break
